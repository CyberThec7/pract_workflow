name: trying secure

on: 
  workflow_dispatch:

jobs: 
  secure:
    runs-on: ubuntu-latest
    steps:
      - name: use real GitHub secret
        run: |
          echo "SECRET_VARIALBE=${{ vars.just_to_tring_secure }}" 
          echo "SECRET_VALUE=${{ secrets.connec }}" 
